@EndUserText.label: 'Access Control'
@MappingRole: true
define role Z4408_AGENCY {
    grant
        select
            on
                Z4408_AGENCY
                   where ( AgencyId ) = aspect pfcg_auth( /LRN/AGCY, /LRN/AGCY, ACTVT = '03' );
                        
}